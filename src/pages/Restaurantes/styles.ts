import styled from "styled-components";

export const ContainerMainRestaurante = styled.ul`
    display: grid;
    grid-template-columns: repeat(2, 474px);
    grid-auto-rows: 472px;
    grid-gap: 32px;
    width: 100%;
    margin: 64px auto;
    padding: 0 !important;
`

const boxCards = styled.section`
    margin: 0 auto;
    display: 100%;
`